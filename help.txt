
console.log("Adding " , numbers + 15)
m.console.log("Concatinating: " + numbers + "15");
Concatinating keeps adding onto a string 
m. Mathematical operators 

console log variable 
    what the expression actually evaultated 

    // Alert 
    alert("we rock");
    //confirm 
     variable doYouRock = confirm("the question is, do *you* rock");

     //Prompt
     var howMuchRock= prompt ("how much do you rock?");

    Create expressions 


    if ((6+5))

    var mixArray= ["Zoo", 12, "Carrot", 3];

    the mixarray will always be called the mixed array 


    var zooAnimals = ["]

    console.log(xXXX.length);


console.log(xXXXX.indexOf(""));

array.push("bear");
.push 
.indexOf

for loops can be used to asked is this less tha n100 



// converts users answer to lowercase

userGuessLower

let 


if users answer is not in the array 
    if (xXXX.indexoF()=== -1) {
        alert ("number must be more than 8 characters");
    }

    // loops through each index of the array 
    for (var i=0; i<vegetables.lenth; i++) {
        console.log("i love " + vegetables[i]);
    }
    interrator 
    loop condition 
    iteration (addition)
for loop run repeated blocks of code over a set period 

    increment

function needs an output so must be missing a return statement example returning 'text' variable

for loop will never excecute before your 'special' variable which is not initialized 

if, else-if, or else (keyword), a condition, and the resulting code in { } curly brackets